services:
  rawon:
    restart: unless-stopped
    image: ghcr.io/stegripe/rawon:latest
    container_name: rawon-bot
    env_file:
      - .env
    volumes:
      - /opt/rawon:/app/cache
    deploy:
      resources:
        limits:
          memory: 512M
